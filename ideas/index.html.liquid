<div class="span-17 last content" id="questions">
    <h3>{{rusic.title}}</h3>
    {% if rusic.summary %}
      {{rusic.summary | markdown}}
    {% endif %}
    {% if current_user and rusic.owner.uuid == current_user.uuid %}
      <p><a href="{{rusic.permalink}}/ideas/new" class="button orange">Add new help item</a></p>
    {% endif %}
    {% if rusic.ideas != empty %}
      <ol>
        {% for idea in rusic.ideas %}
          <li>
            <a href="{{idea.permalink}}">{{idea.title}}</a>
            <span>{{idea.likes}} &hearts;</span>
          </li>
        {% endfor %}
      </ol>
    {% else %}
      <p>
        Sorry, we haven't got any help topics here yet
      </p>
    {% endif %}
</div>
